import TrajectoryGeneration
import trapezoidMotionProfile

class pathCreator: 
    
    def __init__(self, waypoints: list[list[float, float, float, float]], motionProfile: trapezoidMotionProfile): #where index 0 & 1 are x & y, 2 is heading, and 3 is curve intensity.
        self.waypoints
        self.motionProfile